---
const { href, title, body, text, thumbnail } = Astro.props;
---

<article>
  <a href={href}>
    <h2>{title}</h2>
    <img src=`/images/${thumbnail}` alt={title} />
    <p>{text}</p>
  </a>
</article>
